# CLAUDE.md

This repository's agent guidance is maintained in a single source of truth, `AGENTS.md`,
so that every agent tool reads the same rules. Start there, and follow its pointer into
`.agents/` (shared memory + lessons) before non-trivial work.

@AGENTS.md
